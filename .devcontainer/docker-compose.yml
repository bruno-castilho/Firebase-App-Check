version: '3.8'

services:
  app:
    build: .
    volumes:
      - ../:/workspace
      - /var/run/docker.sock:/var/run/docker.sock
    command: sleep infinity
    ports:
      - "3000:3000"
      - "5173:5173"